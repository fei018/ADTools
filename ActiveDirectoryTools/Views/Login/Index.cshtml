@inject IADToolsService _adToolsService
@{
    Layout = null;

    var _adSerivce = _adToolsService.ADService;
    var query = await _adSerivce.GetDomainInfoInDatabase();
    string _domainName = null;
    if (query.Success)
    {
        _domainName = query.Value.DomainName;
    }
}

<!doctype html>
<html class="x-admin-sm">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <meta name="renderer" content="webkit|ie-comp|ie-stand">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width,user-scalable=yes, minimum-scale=0.4, initial-scale=0.8,target-densitydpi=low-dpi" />
    <meta http-equiv="Cache-Control" content="no-siteapp" />
    <link href="~/layui/css/layui.css" rel="stylesheet" media="all"/>
    <link href="~/adtools/login/font.css" rel="stylesheet" />
    <link href="~/adtools/login/login.css" rel="stylesheet">
    <link href="~/adtools/login/xadmin.css" rel="stylesheet">
    <script src="~/layui/layui.all.js"></script>

    <!--[if lt IE 9]>
      <script src="https://cdn.staticfile.org/html5shiv/r29/html5.min.js"></script>
      <script src="https://cdn.staticfile.org/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    <style>
        .domain{
            position:inherit;
            text-align:center;
            font-size:20px;
            color:blueviolet;
        }
    </style>
</head>
<body class="login-bg">
    <div class="login layui-anim layui-anim-up">
        <div class="message">ADTools Login</div>
        <div id="darkbannerwrap"></div>

        <form method="post" class="layui-form" action="/login/dologin">
            <input name="username" placeholder="Login Name" type="text" lay-verify="required" lay-reqText="Required" class="layui-input">
            <hr class="hr15">
            <input name="password" lay-verify="required" placeholder="Password" type="password" lay-reqText="Required" class="layui-input">
            <hr class="hr15">
            <input value="Login" lay-submit lay-filter="login" style="width:100%;" type="submit">
            <hr class="hr20">
            @Html.AntiForgeryToken()
        </form>
        <hr />
        <div class="layui-row">
            <div class="layui-col-xs12 layui-col-md12">
                <div class="domain">@_domainName</div>
            </div>
        </div>
        @if (ViewData["LoginError"] != null)
        {
            <div>
                <p style="color:red">@ViewData["LoginError"]</p>
            </div>
        }
    </div>

    <!-- 底部结束 -->
</body>
</html>